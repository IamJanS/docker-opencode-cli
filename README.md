# docker-opencode-cli
Docker OpenCode container

```
git init
git remote add origin git@github.com:IamJanS/docker-opencode-cli.git
git branch --set-upstream-to=origin/main main
git switch -c main --track origin/main

```
